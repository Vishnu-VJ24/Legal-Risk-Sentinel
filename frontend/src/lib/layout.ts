export const PRIMARY_PANEL_SIZE = {
  defaultSize: '60%',
  minSize: '45%',
  maxSize: '75%',
} as const;

export const SECONDARY_PANEL_SIZE = {
  defaultSize: '40%',
  minSize: '25%',
  maxSize: '55%',
} as const;
