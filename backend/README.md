---
title: Legal Risk Sentinel API
sdk: docker
app_port: 7860
---

# Legal Risk Sentinel API

Mock FastAPI backend for LexAI / Legal Risk Sentinel.

## Endpoints

- `POST /api/analyze`
- `GET /api/results/{session_id}`
- `GET /health`

This Space is intentionally a deterministic mock backend for the portfolio demo so the frontend remains responsive and reliable during recruiter demos.
